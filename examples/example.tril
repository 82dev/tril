struct Vec2{
  x: Int,
  y: Int,
}

fn vec2(x: Int, y: Int) -> Vec2{
  return Vec2{
    x,
    y,
  };
}

fn add_vec2(a: Vec2, b: Vec2) -> Vec2{
  return Vec2{
    a.x + b.x,
    a.y + b.y
  };
}

// fn zero() -> Vec2{
  // return vec2(0, 0);
// }